const express = require('express');
const app =express();
app.listen(500);
app.get('',(req,resp)=>{
   resp.send( `
   <input type="text" placeholder ='User Name'>
   <button>Click Me</button>
   `);
});
