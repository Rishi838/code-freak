class Solution {
public:
    vector<long long> sumOfThree(long long num) {
        long long a=num-3;
        vector<long long> v;
        if(a%3!=0)
        return v;
        a/=3;
        v.push_back(a);
        v.push_back(a+1);
        v.push_back(a+2);
        return v;
    }
};
