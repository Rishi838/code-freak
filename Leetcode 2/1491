class Solution {
public:
    double average(vector<int>& a) {
        sort(a.begin(),a.end());
        double sum=0;
        for(int i=1;i<a.size()-1;i++)
            sum+=a[i];
        return sum /(a.size()-2);
    }
};
