class Solution {
public:
    int findMin(vector<int>& nums) {
        int i=1;
        while(i<nums.size())
        {
          if(nums[i]<nums[i-1])
          return nums[i];
          i++;
        }
        return nums[0];
    }
};
