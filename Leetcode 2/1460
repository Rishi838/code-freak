class Solution {
public:
    bool canBeEqual(vector<int>& target, vector<int>& arr) {
        map<int,int> a,b;
        for(auto i:target)
        a[i]++;
        for(auto i:arr)
        b[i]++;
        return a==b;
    }
};
