#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int n,a;
    cin>>n;
    vector<int> vec1;
    for (int i=0; i<n; i++) {
    cin>>a;
    vec1.push_back(a);
    }  
    sort(vec1.begin(),vec1.end());
   vector<int> :: iterator iter=vec1.begin();
   for (iter; iter != vec1.end(); iter++) {
   cout<<*iter<<" ";
   }
    return 0;
}
