#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    
    int element;
    vector<int>v;
    int n;
    cin>>n;
    while(n>0)
    {
        int a;
        cin>>a;
        v.push_back(a);
        n--;
    }
    
    sort(v.begin(),v.end());
    for(auto element:v)
    {
        cout<<element<<" ";
    }
    
    
    return 0;
}
