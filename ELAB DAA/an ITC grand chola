#include <bits/stdc++.h>
#define m  if(y==0)        ans=x;    else if(y==1)        ans=x+1;else if(y==2)    ans=x+2;else if(x==y)ans=2*x -1;    else    {            ans=4;            ans+=2*(y-2);        ans+=(x-y);        }
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int x,y;
        cin>>x>>y;
        int ans=0;
        m
        cout<<ans<<endl;
        
    }
	return 0;
}
