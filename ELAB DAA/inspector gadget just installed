#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a,b,c,d,e,f;
    cin>>a>>b>>c>>d>>e>>f;
    if(b=="?"){
    cout<<a;
    printf(" %0.2f",-stof(d)*stof(f));
	}
	   if(d=="?"){
    cout<<c;
    printf(" %0.2f",-stof(b)/stof(f));
	}
	   if(f=="?"){
    cout<<e;
    printf(" %0.2f",-stof(b)/stof(d));
	}
	return 0;
}
