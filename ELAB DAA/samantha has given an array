#include <bits/stdc++.h>
#define m   for(int i=1;i<x;i++) {  if(__gcd(y[i-1],y[i])!=1) cout<<"1 "; cout<<y[i];      if(i!=x-1) cout<<" ";}
using namespace std;
int main()
{
  int x,c=0;
  cin>>x;
  int y[x];
  for(int i=0;i<x;i++)
  cin>>y[i];
  for(int i=1;i<x;i++)
  {
      if(__gcd(y[i-1],y[i])!=1)
      c++;
  }
  cout<<c<<endl<<y[0]<<" ";
 m
	return 0;
}
