#include<bits/stdc++.h>
#define m  int a=vec.size()-1,b=0;    while(b<a)    {    if(vec[a]!=vec[b])    return false;    a--;    b++;}    return true;
#define p   if(i==n)    {        int a=vec.size();       if(a>=3 && check(vec))      return true;        return false;    }    vector<int> v=vec;    vec.push_back(arr[i]);    return c(arr,n,i+1,v) || c(arr,n,i+1,vec);
using namespace std;
bool check(vector<int> vec)
{
    m
}
bool c(int arr[],int n,int i,vector<int> vec)
{
   p
}
int main()
{
    int t;
  scanf("%d",&t);
    while(t!=0)
    {
        int n;
       scanf("%d",&n);
        int arr[n];
        for(int i=0;i<n;i++)
        cin>>arr[i];
        vector<int>vec;
        if(c(arr,n,0,vec))
        cout<<"YES\n";
        else
        cout<<"NO\n";
        t--;
    }
	return 0;

}
