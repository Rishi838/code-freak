#include <bits/stdc++.h>
#define m  if(i==1){   if(a>0)   a--; else if(b>0)       {       b--;               c++;   }  else if(c>0)  c--;       else  d+=1;  } else  {           if(b>0)         b--;   else d+=2;   }
using namespace std;

int main()
{
   int n,a,b,c=0,d=0;
   cin>>n>>a>>b;
   int arr[n];
   for(int i=0;i<n;i++)
   cin>>arr[i];
   for(auto i:arr)
   {m
   }
   cout<<d;
	return 0;

}
