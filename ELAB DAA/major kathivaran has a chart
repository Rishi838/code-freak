#include <bits/stdc++.h>
#define a(t)  for(int i=0;i<t;i++)
using namespace std;
int main()
{
  int t;
  cin>>t;
  int arr[t],ans=INT_MAX;
  a(t)
  cin>>arr[i];
  a(t)
  {
      for(int j=i+1;j<t;j++)
      {
        int a=arr[i]-arr[j];
        if(a>=0)
        ans=min(ans,a);
      }
  }
  cout<<ans;
    return 0;
    cout<<"while";
}
