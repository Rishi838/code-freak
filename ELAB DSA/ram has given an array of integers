#include<bits/stdc++.h>
using namespace std;
void func(int num){
    while(num);
}
int main()
{
    int n,sum=0,c=0;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++){
    cin>>arr[i];
    if(arr[i]>=0)
    {
        sum+=arr[i];
        c++;
    }
    }
    cout<<sum<<" "<<c;
    return 0;
}
